---

- name: Create directories for bind volumes
  file:
    path: "{{ nextcloud_basepath }}/{{ item }}"
    state: directory
    owner: www-data
    group: www-data
    mode: '0700'
  loop:
    - nextcloud
    - apps
    - config
    - db
    - data
